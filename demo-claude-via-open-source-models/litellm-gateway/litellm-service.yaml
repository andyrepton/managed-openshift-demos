apiVersion: v1
kind: Service
metadata:
  name: litellm-gateway
  namespace: claude-code-demo
  labels:
    app: litellm-gateway
spec:
  selector:
    app: litellm-gateway
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
  type: ClusterIP
